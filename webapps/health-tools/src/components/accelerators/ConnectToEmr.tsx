import { Container } from "@mui/material";

export const ConnectToEmr = () => {
  return (
    <Container
      id="connect-to-emr-container"
      maxWidth="xl"
      sx={{ display: "flex", flexDirection: "column", height: 1 }}
    ></Container>
  );
};
