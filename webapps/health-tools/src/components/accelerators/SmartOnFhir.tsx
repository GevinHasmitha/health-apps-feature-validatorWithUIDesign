import { Container } from "@mui/material";

export const SmartOnFhir = () => {
  return (
    <Container
      id="smart-on-fhir-container"
      maxWidth="xl"
      sx={{ display: "flex", flexDirection: "column", height: 1 }}
    ></Container>
  );
};
